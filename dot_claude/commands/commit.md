# commit

変更内容をコミットします。以下の手順を順番に実行してください。

## 1. 現状の確認

- `git status` で作業ディレクトリの状態を確認する
- `git diff` および `git diff --cached` で変更内容を確認する
- コミット対象の変更がない場合はその旨を伝えて終了する

## 2. 変更のステージング

- 変更内容を意味のある単位でステージングする
- 変更が多岐にわたる場合は、論理的なまとまりごとに複数コミットに分割する

## 3. コミットメッセージの作成とコミット

コミットメッセージは **Conventional Commits** に従うこと。

### Conventional Commits フォーマット

```
<type>(<scope>): <description>
```

| type     | 用途                                     |
| -------- | ---------------------------------------- |
| feat     | 新機能                                   |
| fix      | バグ修正                                 |
| docs     | ドキュメントのみの変更                   |
| style    | コードの意味に影響しないフォーマット変更 |
| refactor | バグ修正でも機能追加でもないコード変更   |
| perf     | パフォーマンス改善                       |
| test     | テストの追加・修正                       |
| chore    | ビルドプロセスや補助ツールの変更         |
| ci       | CI設定の変更                             |

- **破壊的変更**がある場合は `feat!:` のように `!` を付与するか、フッターに `BREAKING CHANGE:` を記載する
- diffの内容からtypeとscopeを判断し、簡潔で的確なdescriptionを作成する
- コミット完了後、コミットログを表示する

## 4. Pushの確認

- リモートブランチとの差分を確認し、未Pushのコミットがある場合は「リモートにPushしますか？」とユーザーに確認する
- ユーザーが同意した場合は `git push origin HEAD` でPushする
- Push先のリモートブランチが存在しない場合は `git push -u origin HEAD` を使用する
